import { LineUpDirective } from './line-up.directive';

describe('LineUpDirective', () => {
  it('should create an instance', () => {
    const directive = new LineUpDirective();
    expect(directive).toBeTruthy();
  });
});
