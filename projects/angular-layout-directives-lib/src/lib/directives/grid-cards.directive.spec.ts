import { GridCardsDirective } from './grid-cards.directive';

describe('GridCardsDirective', () => {
  it('should create an instance', () => {
    const directive = new GridCardsDirective();
    expect(directive).toBeTruthy();
  });
});
