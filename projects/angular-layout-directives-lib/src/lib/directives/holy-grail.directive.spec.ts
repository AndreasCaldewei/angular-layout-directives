import { HolyGrailDirective } from './holy-grail.directive';

describe('HolyGrailDirective', () => {
  it('should create an instance', () => {
    const directive = new HolyGrailDirective();
    expect(directive).toBeTruthy();
  });
});
