import { PancakeDirective } from './pancake.directive';

describe('PancakeDirective', () => {
  it('should create an instance', () => {
    const directive = new PancakeDirective();
    expect(directive).toBeTruthy();
  });
});
